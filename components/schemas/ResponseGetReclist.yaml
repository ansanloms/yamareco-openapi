allOf:
  - $ref: ./ResponseBase.yaml
  - type: object
    required:
      - reclist
    properties:
      reclist:
        type: array
        description: 山行記録の一覧。
        items:
          $ref: ./RecSummary.yaml
