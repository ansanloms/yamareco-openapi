allOf:
  - $ref: ./ResponseBase.yaml
  - type: object
    required:
      - total
      - comment
    properties:
      total:
        type: integer
        description: コメント数。
      comment:
        type: array
        description: コメントの一覧。
        items:
          $ref: ./Comment.yaml
