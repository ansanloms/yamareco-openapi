allOf:
  - $ref: ./ResponseBase.yaml
  - type: object
    required:
      - poilist
    properties:
      poilist:
        type: array
        description: 地名データの一覧。
        items:
          $ref: ./Poi.yaml
