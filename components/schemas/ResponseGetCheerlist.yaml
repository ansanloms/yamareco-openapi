allOf:
  - $ref: ./ResponseBase.yaml
  - type: object
    required:
      - total
      - userinfo
    properties:
      total:
        type: integer
        description: 拍手数 (こっそり拍手も含む合計)。
      userinfo:
        type: array
        description: 拍手者の一覧。
        items:
          $ref: ./CheerUser.yaml
