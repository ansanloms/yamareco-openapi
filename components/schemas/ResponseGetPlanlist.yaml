allOf:
  - $ref: ./ResponseBase.yaml
  - type: object
    required:
      - total
      - planlist
    properties:
      total:
        type: integer
        description: 全件数。
      planlist:
        type: array
        description: 山行計画の一覧。
        items:
          $ref: ./PlanSummary.yaml
